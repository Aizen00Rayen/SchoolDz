REACT_APP_BACKEND_URL=https://scolaris.cloud
# Don't ship a webpack sourcemap in the production build — it let anyone
# reconstruct the entire unminified frontend source (API client structure,
# auth flow, internal file layout) via a publicly-fetchable .js.map file.
GENERATE_SOURCEMAP=false
